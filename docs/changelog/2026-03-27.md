# Report — 27.03.2026

## Summary

- 🐛 Fixed follow-up analysis page and follow-up detail items
- 🐛 Fixed agents pages — profiles list and agent profile form
- ✨ Extracted `AgendaItem` into a standalone component for the main dashboard
- 🎨 Improved `MethodologyCriteria` artifact — color-coded scoring, richer
  tables, progress bars with status icons
- 🏗️ Calendar test fix

---

### ✨ New features / improvements

- **Color-coded scoring in MethodologyCriteria artifact**: Scoring tables now
  detect "fact" and "max" columns and highlight the score cell in green / yellow
  / red depending on the percentage (≥90 emerald, ≥75 green, ≥60 yellow, below
  red). Progress-summary blocks now display a large bold score, a status label
  ("Good" / "Satisfactory" / "Needs improvement"), and a colored progress bar
  with the same thresholds.

- **Header blocks gain a TrendingUp icon**: `header` blocks in the
  `MethodologyCriteria` artifact now render with a small `TrendingUp` icon from
  `lucide-react` for visual hierarchy.

### 🐛 Bug fixes

- **Follow-up analysis page**: Fixed data loading and rendering on the
  `/dashboard/follow-ups/analysis/[id]` page — corrected props passed to the
  analysis component and adjusted how the linked meeting is resolved.

- **Follow-up item**: Fixed `follow-up-item.tsx` — corrected type references
  (aligned with updated `follow-up/model/types.ts`) and resolved display issues
  in the follow-up list.

- **Agent profiles list**: Fixed `agent-profiles-list.tsx` to correctly pass
  props; added missing field to `features/agents/model/types.ts`.

- **Agent profile form**: Fixed `agent-profile-form.tsx` — added missing form
  state handling.

- **Calendar tests**: Updated `event.test.tsx` to match current component API
  (three failing assertions corrected).

### ♻️ Refactoring

- **`AgendaItem` extracted**: The agenda card that was inline in
  `upcoming-agendas-block.tsx` has been moved to its own
  `features/main-dashboard/ui/agenda-item.tsx` component. The block component is
  now significantly smaller and only handles the outer layout and the list
  iteration.

### 🏗️ Infrastructure

- Lint/format auto-fix pass on `entities/artifact/ui/methodology-criteria.tsx`
  (pre-push hook) — no behavior changes.
