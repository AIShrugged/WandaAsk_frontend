# Report — 30.03.2026

## Summary

- ✨ Issues table: column sorting, "Assignee: Me" filter, search by name with
  debounce and URL sync
- ✨ Issues: type filter (Development / Organization), default assignee filter
  to current user
- 🐛 Fixed TaskTracker and Kanban board

---

### ✨ New features

- **Column sorting in Issues table**: `SortableHeader` component added; clicking
  column headers toggles asc/desc sort, synced to URL params.

- **"Assignee: Me" filter**: Quick filter to show only issues assigned to the
  current user. Defaults to active when no assignee param is in the URL
  (`shared/lib/getCurrentUserId.ts` added).

- **Search by name**: Debounced text search for issues, state synced to URL.

- **Issue type filter**: New `IssueType` union
  (`'Development' | 'Organization'`); filter dropdown added to the issues
  toolbar.

### 🐛 Bug fixes

- **TaskTracker / Kanban**: Fixed broken data loading in
  `features/kanban/api/kanban.ts`; removed stale dead state (`orgOptions`,
  `teamOptions`, team-loading) from `kanban-board.tsx`.
